[project]
name = "attention2d"
version = "0.1.0"
description = "Tiny 2D prefill and lattice Attention Residuals torch prototype."
requires-python = ">=3.10"
dependencies = ["torch"]

[tool.pytest.ini_options]
testpaths = ["tests"]
